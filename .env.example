# 任意の環境変数（実シークレットはコミットしない）
# コピーして使う例: cp .env.example .env

# OpenAI API キー（任意）
# 設定すると AI-assisted resolver が有効になる。
# 未設定でも heuristic resolver で pnpm demo は通る。
OPENAI_API_KEY=

# 将来用（未使用）: モデル名を上書きしたい場合のプレースホルダ
# OPENAI_MODEL=gpt-4o-mini
